import React from 'react'

const Task22 = () => {
    return (
        <div>
            <p><strong>Вбудований (Inline) стиль</strong> – застосовується безпосередньо до конкретного HTML-елемента за
                допомогою атрибута <code>style</code>. Цей метод використовується для швидкої зміни стилю окремого
                елемента, але не є зручним для масштабних проектів, оскільки ускладнює управління стилями та
                унеможливлює їх повторне використання.</p>

            <p><strong>Внутрішнє (Embedded) підключення</strong> – використовується шляхом додавання CSS-коду
                у <code>&lt;style&gt;</code> всередині <code>&lt;head&gt;</code> HTML-документа. Цей метод дозволяє
                централізовано визначати стилі для всієї сторінки, але не підходить для проєктів із великою кількістю
                сторінок, оскільки кожна сторінка потребує повторного визначення стилів.</p>

            <p><strong>Зовнішнє (External) підключення</strong> – передбачає використання окремого CSS-файлу, який
                підключається до HTML-документа за допомогою елемента <code>&lt;link&gt;</code>. Це найоптимальніший
                спосіб організації стилів, оскільки дозволяє використовувати один CSS-файл для кількох сторінок, що
                спрощує оновлення та підтримку коду.</p>

            <p><strong>Імпортований (Import) стиль</strong> – використовується для підключення зовнішніх CSS-файлів
                всередині іншого CSS-файлу за допомогою правила <code>@import</code>. Це дозволяє розбивати стилі на
                окремі файли та організовувати їх у структуровані модулі, що особливо корисно у великих проєктах.</p>

            <p><strong>Підключення через CSS-фреймворки</strong> – передбачає використання готових бібліотек стилів,
                таких як Bootstrap або Tailwind, які підключаються через CDN або локальні файли. Це дозволяє швидко
                застосовувати професійно розроблені стилі без необхідності писати їх з нуля.</p>

        </div>
    )
}
export default Task22
